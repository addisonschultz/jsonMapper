var fs = require("fs"),
  path = require("path");

function reducer(jsonFile) {
  Object.keys(jsonFile).map(function (key, index) {
    return {
      /**
       *
       * I'll map over the data and replace it to a new
       * object structure here, I can do this part
       *
       */
    };
  });
}

reducer();
